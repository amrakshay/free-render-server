# Build-time Configuration (affects Docker image size and capabilities)
# Set these before running ./build_image.sh or docker-compose build
INSTALL_N8N=false
INSTALL_NGROK=true

# Runtime Service Configuration
N8N_ENABLED=false
NGROK_ENABLED=false
NGROK_AUTHTOKEN=

# Build Options:
# - INSTALL_N8N=true   : Include n8n workflow automation (adds ~300MB)
# - INSTALL_N8N=false  : Minimal image without n8n (nginx + SSH + Python only)
# - INSTALL_NGROK=true : Include ngrok for SSH tunneling (adds ~50MB)
# - INSTALL_NGROK=false: No tunneling capability

# Runtime Options:
# - N8N_ENABLED=true   : Start n8n service (requires INSTALL_N8N=true)
# - NGROK_ENABLED=true : Create SSH tunnel (requires INSTALL_NGROK=true)

# To enable ngrok SSH tunnel, set NGROK_ENABLED=true
# This will create a TCP tunnel for SSH access on port 22
# To use a persistent tunnel (recommended), add your ngrok auth token
# Get your auth token from: https://dashboard.ngrok.com/get-started/your-authtoken

# Example configurations:
# 1. Full setup with persistent tunnel:
#    INSTALL_N8N=true, INSTALL_NGROK=true, N8N_ENABLED=true, NGROK_ENABLED=true, NGROK_AUTHTOKEN=your_token

# 2. Minimal Python/nginx server only:
#    INSTALL_N8N=false, INSTALL_NGROK=false, N8N_ENABLED=false, NGROK_ENABLED=false

# 3. n8n workflow server without tunneling:
#    INSTALL_N8N=true, INSTALL_NGROK=false, N8N_ENABLED=true, NGROK_ENABLED=false

# SSH Access:
# Default credentials: root / Secure@FreeRender2024
# Change the password after first login for security!
